{
    "private": true,
    "workspaces": {
        "packages": [
            "packages/*"
        ]
    },
    "scripts": {
        "start": "nodemon src/index.js --ext js  --exec babel-node",
        "heroku-postbuild": "cd packages/frontend && yarn install && npm run build && mkdir ../backend/dist/public && cp -R build/*.* ../backend/dist/public && cp -R build/static ../backend/dist/public/static && mv ../backend/dist/public/index.html ../backend/dist && cd ../backend && yarn install && cd ../.. && yarn install"
      },
    "engines": {
        "node": "12.13.0",
        "npm": "6.x",
        "yarn": "1.x"
    }
}
